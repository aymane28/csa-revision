{
  "name": "csa-revision-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "generate-vapid-keys": "node scripts/generate-vapid-keys.mjs"
  },
  "dependencies": {
    "webpush-webcrypto": "^1.0.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250718.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
